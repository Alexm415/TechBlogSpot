<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <title>TechBlogSpot</title>
  </head>
  <body>
    <div class="container">
      <header class="blog-header py-3">
        <div
          class="row flex-nowrap justify-content-between align-items-center"
        ></div>
      </header>
      <div class="nav-scroller py-1 mb2">
        <nav class="nav d-flex justify-content-between">
          <a class="p-2 text-muted" href="/">Home</a>
          {{#if logged_in}}
            <a class="p-2 text-muted" href="/profile">profile</a>

            <a class="p-2 text-muted no-button" id="logout">logout</a>
          {{else}}
            <a class="p-2 text-muted" href="/login">login</a>
          {{/if}}
        </nav>
      </div>
      <div class="jumbotron p-3 p-md-5 text-white rounded bg-dark">
        <div class="col-md-6 px-0">
          <h1 class="display-4 font-italic">Welcome to the TechBlogSpot</h1>
          <p class="lead my-3">Where you can talk about everything and anything
            tech or other thing in your life!</p>

        </div>
      </div>

    </div>
    {{{body}}}
  </body>
  <script src="/js/logout.js"></script>
</html>